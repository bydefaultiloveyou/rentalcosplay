 <div>
     <label for="email" class="sr-only">{{ $labelText }}</label>
     <div class="relative">
         <input type="{{ $type }}" name="{{ $name }}"
             class="w-full rounded-lg border-gray-200 p-4 pe-12 text-sm" placeholder="{{ $placeholder }}" />
     </div>
 </div>
